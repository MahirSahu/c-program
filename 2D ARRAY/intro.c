#include<stdio.h>
int main()
{
    // 2d arrays are type of matrix
    //  datatype array_name[r][c]
    // here r= rows of matrix and c = columns of a matrix
    // arr[0][1] - signifies the value of element in 1st row and 2nd column 

   return 0;

}